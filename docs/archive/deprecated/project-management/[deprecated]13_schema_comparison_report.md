# [deprecated] 기존 스키마 vs MVP 스키마 비교 분석 보고서

> **Deprecated 날짜**: 2025-12-28  
> **Deprecated 이유**: 이 보고서는 특정 시점(2025-10-18)의 스키마 비교 분석 결과를 기록한 것으로, 현재는 Phase 4+ 개발이 진행 중이며 더 최신 상태 정보는 readme.md와 최신 문서들을 참조해야 함.  
> **분석일**: 2025-10-18  
> **분석자**: AI Assistant  
> **목적**: 기존 `create_db.sql`과 새로운 `mvp_schema.sql` 간의 차이점 및 호환성 분석

## 🔍 **핵심 차이점 분석**

### **1. 아키텍처 접근 방식의 차이**

#### **기존 스키마 (create_db.sql)**
- **복잡한 3-tier 구조**: game_data → reference_layer → runtime_data
- **세션 중심 설계**: 모든 런타임 데이터가 세션에 종속
- **상태 기반 관리**: entity_states, object_states 테이블로 상태 분리
- **참조 레이어 의존**: 모든 런타임 엔티티가 reference_layer를 거쳐야 함

#### **MVP 스키마 (mvp_schema.sql)**
- **단순화된 구조**: game_data → runtime_data 직접 연결
- **엔티티 중심 설계**: 런타임 엔티티를 직접 관리
- **직접 관리**: runtime_entities, runtime_cells 테이블로 직접 관리
- **참조 레이어 최소화**: promote 기능을 위한 최소한의 참조만 유지

### **2. 테이블 구조 비교**

#### **기존 스키마의 런타임 테이블**
```sql
-- 기존: 복잡한 참조 구조
runtime_data.active_sessions
reference_layer.entity_references  -- 중간 참조 테이블
reference_layer.cell_references    -- 중간 참조 테이블
reference_layer.object_references  -- 중간 참조 테이블
runtime_data.entity_states         -- 상태만 저장
runtime_data.object_states         -- 상태만 저장
```

#### **MVP 스키마의 런타임 테이블**
```sql
-- MVP: 직접 관리 구조
runtime_data.active_sessions
runtime_data.runtime_entities      -- 엔티티 직접 저장
runtime_data.runtime_cells         -- 셀 직접 저장
runtime_data.runtime_objects       -- 오브젝트 직접 저장
runtime_data.runtime_cell_entities -- 관계 테이블
runtime_data.runtime_cell_objects  -- 관계 테이블
```

### **3. 구현된 Manager와의 호환성**

#### **EntityManager 호환성**
- **기존 스키마**: ❌ 호환 불가
  - `runtime_entities` 테이블이 존재하지 않음
  - `entity_states` 테이블만 존재하지만 구조가 다름
  - 복잡한 참조 구조로 직접 접근 불가

- **MVP 스키마**: ✅ 완벽 호환
  - `runtime_entities` 테이블과 정확히 일치
  - 모든 필드와 타입이 호환
  - 직접적인 CRUD 작업 가능

#### **CellManager 호환성**
- **기존 스키마**: ❌ 호환 불가
  - `runtime_cells` 테이블이 존재하지 않음
  - `cell_references`를 통한 간접 접근만 가능
  - 복잡한 조인 쿼리 필요

- **MVP 스키마**: ✅ 완벽 호환
  - `runtime_cells` 테이블과 정확히 일치
  - 모든 필드와 타입이 호환
  - 직접적인 CRUD 작업 가능

#### **ActionHandler 호환성**
- **기존 스키마**: ❌ 호환 불가
  - `action_logs` 테이블이 존재하지 않음
  - 로그 기록 기능이 없음

- **MVP 스키마**: ✅ 완벽 호환
  - `action_logs` 테이블과 정확히 일치
  - 모든 필드와 타입이 호환

#### **DialogueManager 호환성**
- **기존 스키마**: ✅ 부분 호환
  - `dialogue_history` 테이블이 존재
  - 구조가 약간 다르지만 호환 가능

- **MVP 스키마**: ✅ 완벽 호환
  - `dialogue_history` 테이블과 정확히 일치
  - 모든 필드와 타입이 호환

### **4. 데이터 흐름 비교**

#### **기존 스키마 데이터 흐름**
```
Game Data → Reference Layer → Runtime States
    ↓           ↓              ↓
entities → entity_references → entity_states
cells    → cell_references   → (상태 없음)
objects  → object_references → object_states
```

#### **MVP 스키마 데이터 흐름**
```
Game Data → Runtime Data (직접)
    ↓           ↓
entities → runtime_entities
cells    → runtime_cells
objects  → runtime_objects
```

### **5. 성능 및 복잡성 비교**

#### **기존 스키마**
- **장점**: 
  - 데이터 무결성 보장 (참조 무결성)
  - 세션별 데이터 격리
  - 복잡한 상태 관리 가능
- **단점**:
  - 복잡한 조인 쿼리 필요
  - 성능 오버헤드 (참조 테이블)
  - 구현 복잡성 증가

#### **MVP 스키마**
- **장점**:
  - 단순한 직접 접근
  - 높은 성능 (조인 최소화)
  - 구현 단순성
  - Manager와 완벽 호환
- **단점**:
  - 데이터 중복 가능성
  - 참조 무결성 관리 필요

### **6. MVP 요구사항 충족도**

#### **기존 스키마**
- ❌ **EntityManager 호환**: 불가능
- ❌ **CellManager 호환**: 불가능  
- ❌ **ActionHandler 호환**: 불가능
- ✅ **DialogueManager 호환**: 부분 가능
- ❌ **즉시 사용 가능**: 불가능

#### **MVP 스키마**
- ✅ **EntityManager 호환**: 완벽
- ✅ **CellManager 호환**: 완벽
- ✅ **ActionHandler 호환**: 완벽
- ✅ **DialogueManager 호환**: 완벽
- ✅ **즉시 사용 가능**: 완벽

## 🎯 **결론 및 권장사항**

### **핵심 발견사항**
1. **기존 스키마는 구현된 Manager들과 호환되지 않음**
2. **MVP 스키마는 모든 Manager와 완벽 호환**
3. **기존 스키마는 과도하게 복잡한 구조**
4. **MVP 스키마는 MVP 요구사항에 최적화**

### **권장사항**

#### **1. MVP 스키마 채택**
- **이유**: 구현된 모든 Manager와 완벽 호환
- **장점**: 즉시 사용 가능, 높은 성능, 단순한 구현
- **단점**: 미래 확장성 제한 (하지만 MVP에는 충분)

#### **2. 기존 스키마 보존**
- **이유**: 미래 확장성을 위한 참고 자료
- **용도**: v2.0 이상에서 고급 기능 구현 시 참조
- **위치**: `database/create_db.sql` (백업용)

#### **3. 점진적 마이그레이션 계획**
- **Phase 1**: MVP 스키마로 MVP 완성
- **Phase 2**: 필요시 기존 스키마 구조로 마이그레이션
- **Phase 3**: 하이브리드 구조 (최적의 두 방식 결합)

## ✅ **최종 권장사항**

**MVP 스키마 (`database/mvp_schema.sql`)를 즉시 채택하여 DB 생성을 진행하세요.**

**근거**:
1. **100% 호환성**: 모든 구현된 Manager와 완벽 호환
2. **즉시 사용 가능**: 추가 수정 없이 바로 사용 가능
3. **MVP 최적화**: MVP 요구사항에 최적화된 구조
4. **성능 우수**: 단순한 구조로 높은 성능
5. **개발 효율성**: 복잡성 최소화로 개발 속도 향상

**기존 스키마는 미래 확장성을 위한 참고 자료로 보존하되, 현재 MVP 개발에는 MVP 스키마를 사용하는 것이 최적입니다.**
