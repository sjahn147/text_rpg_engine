# RPG Engine 데이터베이스 설정
# 이 파일을 .env로 복사하고 실제 값으로 수정하세요

# 데이터베이스 연결 설정
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=2696Sjbj!
DB_NAME=rpg_engine

# 게임 설정
MAX_PLAYERS_PER_SESSION=1
SAVE_INTERVAL_SECONDS=300
SESSION_TIMEOUT_MINUTES=60
DEFAULT_INVENTORY_SIZE=20

# 로그 설정
LOG_LEVEL=INFO
LOG_TO_FILE=true
LOG_TO_CONSOLE=true

# 개발 설정
DEBUG_MODE=false
DEV_MODE_ENABLED=true
